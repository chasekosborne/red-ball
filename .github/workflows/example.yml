# Example yml file for automation
name: Example Workflow

on:
  workflow_dispatch:

jobs:
  example_workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Get our files
        uses: actions/checkout@v2
      - name: Check out the VM
        run: |
          echo $ ls -la
          ls -la
          echo $ whoami
          whoami
      - name: fetch keys
        uses: uname/ssh-key-action/@v2
        with:
          key: ${{secrets.ID_RSA}}$
          name: id_rsa 
          known_hosts: ${{ secrets.KNOWN_HOSTS }}
      - name: login
        run : |
          echo $ ssh uname@cassini.cs.kent.edu
          ssh uname@cassini.cs.kent.edu

          echo $ ssh uname@cassini.cs.kent.edu 
          ssh uname@cassini.cs.kent.edu

